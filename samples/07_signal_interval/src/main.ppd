// ── 07_signal_interval triggered by every N ms ────────────────────────────

var cooldown: int = 10

@interval(100)
fun on_timer()
    print_int(cooldown)
    print_str("\n")
    cooldown -= 1
    if cooldown <= 0
        exit()
