// ── Opcodes ───────────────────────────────────────────────────────────────────

enum OpCode
    // Immediate
    Push         = 0x01  // PUSH i32 (4-byte little-endian)
    // Global data segment
    LoadGlobal      = 0x10  // LOAD_GLOBAL addr(u16)  → push data[addr] as i32
    StoreGlobal     = 0x11  // STORE_GLOBAL addr(u16) → pop i32 → data[addr]
    LoadGlobalInd   = 0x14  // LOAD_GLOBAL_IND  → pop addr, push data[addr] as i32
    StoreGlobalInd  = 0x15  // STORE_GLOBAL_IND → pop addr, pop val, data[addr] = val
    // Stack
    Dup          = 0x02
    Swap         = 0x03
    Drop         = 0x04
    // Integer arithmetic
    AddInt       = 0x20
    SubInt       = 0x21
    MulInt       = 0x22
    DivInt       = 0x23
    ModInt       = 0x24
    NegInt       = 0x25
    ShlInt       = 0x26
    ShrInt       = 0x27  // logical, zero-fill
    SarInt       = 0x28  // arithmetic, sign-fill
    And          = 0x29
    Or           = 0x2A
    Xor          = 0x2B
    Not          = 0x2C
    // Fixed-point arithmetic (16.16)
    AddFixed     = 0x40
    SubFixed     = 0x41
    MulFixed     = 0x42  // (a * b) >> 16 via i64
    DivFixed     = 0x43  // (a << 16) / b via i64
    NegFixed     = 0x44
    IntToFixed   = 0x45  // a << 16
    FixedToInt   = 0x46  // a >> 16
    // Integer comparison
    CmpEqInt     = 0x50
    CmpNeInt     = 0x51
    CmpLtInt     = 0x52
    CmpLeInt     = 0x53
    CmpGtInt     = 0x54
    CmpGeInt     = 0x55
    // Fixed-point comparison
    CmpEqFixed   = 0x56
    CmpNeFixed   = 0x57
    CmpLtFixed   = 0x58
    CmpLeFixed   = 0x59
    CmpGtFixed   = 0x5A
    CmpGeFixed   = 0x5B
    // Control flow
    Jmp          = 0x60  // JMP addr (u16)
    JmpIfTrue    = 0x61  // JMP_IF_TRUE addr (u16)
    JmpIfFalse   = 0x62  // JMP_IF_FALSE addr (u16)
    Call         = 0x63  // CALL addr (u16); args are dropped, no locals
    Ret          = 0x64  // RET
    // Module dispatch
    CallModule   = 0x70  // CALL_MODULE module_id(u8) sub(u8)

// ── Built-in module IDs ───────────────────────────────────────────────────────

const MODULE_CONSOLE: u8 = 0x01
const MODULE_EVENT:   u8 = 0x02

// ── Console sub-codes (MODULE_CONSOLE) ───────────────────────────────────────

enum ConsoleCode
    PrintInt     = 0x01
    PrintFixed   = 0x02
    PrintStr     = 0x03

// ── Event sub-codes (MODULE_EVENT) ────────────────────────────────────────────

enum EventCode
    CreateHandler = 0x01
    ExitHandler   = 0x02
    HandlerSleep  = 0x03
    Send          = 0x04
